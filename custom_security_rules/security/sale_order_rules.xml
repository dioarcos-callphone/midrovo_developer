<odoo>
    <data noupdate="1">
        <!-- Regla de registro para restringir las ventas solo al usuario específico -->
        <record id="sale_order_user_rule" model="ir.rule">
            <field name="name">Sales Order User Rule</field>
            <field name="model_id" ref="sale.model_sale_order"/>
            <field name="domain_force">[('user_id', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('group_custom_group'))]"/>
        </record>
    </data>
</odoo>
