<odoo>
    <data noupdate="1">
        <!-- Regla de registro para restringir los contactos solo al usuario específico -->
        <record id="res_partner_user_rule" model="ir.rule">
            <field name="name">Contact User Rule</field>
            <field name="model_id" ref="base.model_res_partner"/>
            <field name="domain_force">[('create_uid', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('group_custom_group'))]"/>
        </record>
    </data>
</odoo>
