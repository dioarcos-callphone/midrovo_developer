<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <data>
    
        <record id="account_move_view_form_inherit" model="ir.ui.view">
            <field name="name">account.move.view.form.inherit</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <xpath expr="//header" position="attributes">
                    <attribute name="groups">!electronic_document_portal.portal_user_internal_group</attribute>
                </xpath>
                <xpath expr="//header" position="after">
                    <header groups='electronic_document_portal.portal_user_internal_group'>
                        <!-- Send (only invoices) -->
                        <button name="action_invoice_sent"
                                type="object"
                                string="Enviar e Imprimir"
                                attrs="{'invisible':['|', '|', ('state', '!=', 'posted'), ('is_move_sent', '=', True), ('move_type', 'not in', ('out_invoice', 'out_refund', 'in_invoice'))]}"
                                class="oe_highlight"
                                data-hotkey="y"/>
                        <button name="action_invoice_sent"
                                type="object"
                                string="Enviar e Imprimir"
                                class="oe_highlight"
                                attrs="{'invisible':['|', '|', ('state', '!=', 'posted'), ('is_move_sent', '=', False), ('move_type', 'not in', ('out_invoice', 'out_refund', 'in_invoice', 'in_refund'))]}"
                                data-hotkey="y"/>
                        <button name="print_document" string='Imprimir RIDE'
							type='object' groups="account.group_account_invoice"
							class="oe_highlight"/>
                        <field name="state" widget="statusbar" statusbar_visible="draft,posted"/>
                    </header>
                </xpath>

                <xpath expr="//button[@name='action_invoice_sent']" position="replace">
                    <button name="action_invoice_sent"
                            type="object"
                            string="Enviar e Imprimir"
                            attrs="{'invisible':['|', '|', ('state', '!=', 'posted'), ('is_move_sent', '=', True), ('move_type', 'not in', ('out_invoice', 'out_refund', 'in_invoice'))]}"
                            class="oe_highlight"
                            data-hotkey="y"/>
                </xpath>
                
            </field>
        </record>

        <record id="account_invoice_sales_form_view_inherit" model="ir.ui.view">
            <field name="name">account.invoice.sales.form.view.inherit</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="ec_account_edi.account_invoice_sales_form_view"/>
            <field name="arch" type="xml">
                <!-- Agregar un notebook -->
                <notebook position="inside">
                    <page string="SRI Messages">
                        <field name="sri_message_ids">
                            <tree string="Mensajes Informativos">
                                <field name="message_code_id" />
                                <field name="message_type" />
                                <field name="message" />
                                <field name="other_info" />
                                <field name="create_date" />
                                <field name="write_date" />
                            </tree>
                        </field>
                    </page>
                    <page string="SRI Info">
                        <field name="xml_data_id" mode="form" options="{'no_open': True}">
                            <form>
                                <group>
                                    <field name="response_date" />
                                </group>
                            </form>
                        </field>
                    </page>
                </notebook>
            </field>
        </record>
    
    </data>
    

</odoo>
