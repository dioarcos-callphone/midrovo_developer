<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <!-- Regla: Acceso limitado a cartera -->
        <record id="rule_saldo_cartera_limited" model="ir.rule">
            <field name="name">rule.saldo.cartera.limited</field>
            <field name="model_id" ref="model_balance_portfolio"/>
            <field name="groups" eval="[(4, ref('balance_portfolio.group_saldo_cartera_limited'))]"/>
            <field name="domain_force">[('sales_man', '=', user.balance_code)]</field>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="False"/>
        </record> 

    </data>
</odoo>